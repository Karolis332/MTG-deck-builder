---
phase: 01-ai-provider-upgrades
task: 9
total_tasks: 11
status: in_progress
last_updated: 2026-04-02T00:00:00Z
---

<current_state>
Session ended at ~90% context. 8 of 11 tasks done and committed (56e2f97).
Working directory is clean. CF API is back online and pipeline is running.
3 small upgrade tasks remain before this phase is complete.
</current_state>

<completed_work>

- Task 1: Restored CF API (was down 37h — api + worker containers were stopped) ✓
- Task 2: Triggered CF pipeline + model retrain (175K+ decks, running at pause time) ✓
- Task 3: Committed 5 modified files — Groq/xAI cascade in chat/route.ts, settings-dialog.tsx, max_tokens fix ✓
- Task 4: Cleaned up .gitignore — Cabbage Merchant scratch, diag scripts, match frames ✓
- Task 5: Fixed getPreferredProvider() type cast (missing 'xai') ✓
- Task 6: Wired Groq/xAI into ai-deck-builder.ts with full cascade + callOpenAICompatible() ✓
- Task 7: Added Test Connection buttons for Groq and xAI (server-side via /api/settings/test-provider) ✓
- Task 8: CF model version badge in settings dialog (loaded on open via /health endpoint) ✓
</completed_work>

<remaining_work>

- Task 9 (HIGH - correctness bug): Groq candidate truncation
  - ai-deck-builder.ts: detect when active provider is Groq and slice candidates to 70 instead of 120
  - Groq free tier caps output at ~6K tokens; 120 candidates causes mid-JSON truncation → bad decks
  - Location: buildDeckWithAI(), the `const candidates = poolResult.pool.slice(0, 120)` line

- Task 10 (LOW - 1 line): CF retrain date in test result
  - settings-dialog.tsx line ~320: setCfMessage currently shows deck_count + model_version
  - Add last_retrained: format as relative date ("retrained 3 days ago")
  - data.last_retrained is an ISO string from the /health endpoint

- Task 11 (MEDIUM): Gemini 2.5 Flash support
  - Google AI API is OpenAI-compatible: https://generativelanguage.googleapis.com/v1beta/openai
  - Model: gemini-2.5-flash-preview-04-17 (or latest)
  - Add to: ai-deck-builder.ts (getGeminiKey + cascade), chat/route.ts (same), settings-dialog.tsx (key field + test button), /api/settings/test-provider/route.ts (gemini case)
  - Slots into the existing cascade pattern cleanly
</remaining_work>

<decisions_made>

- Provider cascade order: Claude → OpenAI → Groq → xAI → QuickBuild (paid/best first, free as fallback)
- Test connection is server-side (/api/settings/test-provider) — Groq/xAI don't allow browser CORS
- Groq model: llama-3.3-70b-versatile, xAI: grok-3-mini-fast, Gemini: gemini-2.5-flash-preview
- CF API SSH key is at ~/.ssh/id_ed25519_geo_vps for future VPS access
</decisions_made>

<blockers>
None blocking. CF pipeline running in background (not blocking code work).
</blockers>

<context>
This session added Groq + xAI as free AI provider alternatives throughout the app.
The architecture is a clean cascade pattern — each provider is a ProviderEntry with a call() function.
Adding Gemini follows the exact same pattern as Groq/xAI.
The test-provider API route (src/app/api/settings/test-provider/route.ts) just needs a new `if (provider === 'gemini')` block.
</context>

<next_action>
/clear → then:
1. Read src/lib/ai-deck-builder.ts around line 355 (candidates slice)
2. Read src/components/settings-dialog.tsx around line 315-325 (setCfMessage) and line 960-966 (after xAI section for new Gemini field)
3. Read src/app/api/settings/test-provider/route.ts (full, it's short)
4. Make all 3 changes + commit
</next_action>
